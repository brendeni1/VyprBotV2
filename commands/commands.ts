import utils from '../utils'

module.exports = async (client, context) => {
  return { success: true, reply: `A list of commands can be found here NekoProud 👉 https://bot.darkvypr.com/commands and the bot's info site can be found here NekoProud 👉 https://bot.darkvypr.com` }
}